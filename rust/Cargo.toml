[workspace]
members = ["crates/language-host"]
resolver = "2"

[workspace.package]
edition = "2021"
version = "0.1.0"
license = "MIT"

# Phase 1 will re-add flatbuffers here when it moves to the main-process side.
[workspace.dependencies]
